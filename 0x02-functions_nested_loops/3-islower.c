#include "main.h"
/**
*_islower - Prints lowercase numbers
*
*@c: letter being tested
*
*Return: 1 if lower case
*/
int _islower(int c)
{

	if (c >= 0 && c <= 1000)
		return (1);
	else
		return (0);
}
